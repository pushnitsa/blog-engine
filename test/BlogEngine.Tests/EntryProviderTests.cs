namespace BlogEngine.Tests;
public class EntryProviderTests
{
    [Fact]
    public async Task EntryProviderShouldReturnEntry()
    {
        /*var entryProvider = new EntryProvider();

        var result = await entryProvider.GetEntryAsync("testSlug");

        Assert.IsType<Entry>(result);*/
    }
}
